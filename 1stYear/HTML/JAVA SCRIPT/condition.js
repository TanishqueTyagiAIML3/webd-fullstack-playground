const val=0;
// if(val==10)
// {
//     document.write(`Sachin is ${val}`);
// }
// else
//     document.write(`Tanishque is best than Sachin and Sachin is not ${val}`);

switch(val)
{
    case 0:
        document.write( "Enter the value"  );
        break;
        default:
            document.write("Young boy");
        
}